# Configuration and interfaces

Catalog only developer-facing surfaces. Source definitions remain authoritative.

| Entry | Owner | Type | Default | Allowed values | Scope | Effect | Compatibility notes | Source | Validation path |
|---|---|---|---|---|---|---|---|---|---|
| `CACHEROUTE_RUNTIME_PROFILE` | Scheduler/Proxy observability startup | Environment variable | service code falls back through legacy startup resolution | `auto`, `legacy`, `v1`, `test/mock` plus compatibility aliases accepted by normalizer | Process startup | Resolves trace runtime metadata | Current Scheduler and Proxy AUTO resolution uses `v1_available=False` and resolves to legacy unless explicit metadata is supplied | `src/cacheroute/observability/startup.py` | `test/observability` |
| `CACHEROUTE_TRACE_SAMPLE_RATE` | Scheduler/Proxy observability startup | Environment variable | `0.0` when absent/malformed | finite float `0.0` to `1.0` | Process startup | Controls deterministic trace sampling flag | Malformed values fail closed and report warning reason | `src/cacheroute/observability/propagation.py` | `test/observability` |
| RuntimeProfile wire values | Canonical runtime | enum/string | none | `v1`, `legacy`, `test/mock`; `auto` startup-only | Contracts and startup | Describes active runtime compatibility profile | `modern` normalizes to `v1` through compatibility helpers | `src/cacheroute/runtime/profiles.py` | `test/test_runtime_compat.py` |
| Injection modes | Scheduler/Proxy request paths | request field | Existing path usually defaults to `kvcache` when absent unless target path says otherwise | `kvcache`, `text`, `hybrid` | Request handling | Chooses knowledge injection/recomputation behavior | Do not spread new mode semantics across many files | component source and READMEs | demos |
| Scheduler service plane | Scheduler | HTTP port | `7001` | host:port | Service | OpenAI-compatible `/v1/chat/completions`, `/v1/completions`; debug endpoints | Root service remains Transitional | `README.md`, `scheduler/README.md` | demo scheduler |
| Scheduler control plane | Scheduler | HTTP port | `7002` | host:port | Control | Proxy/KDN registration and heartbeat | Transitional root service | `scheduler/README.md` | demos |
| Proxy service plane | Proxy | HTTP port | `8001` | host:port | Service | Receives Scheduler requests and forwards to Instance | Reserved trace headers are Scheduler-owned | `README.md`, `proxy/README.md` | demo proxy |
| Proxy control plane | Proxy | HTTP port | `8002` | host:port | Control | Instance registration/list/resource APIs | Transitional root service | `proxy/README.md` | demo proxy |
| Proxy UI | UI/Proxy | HTTP port | `8202` | host:port | UI | Shows Proxy control-plane and resource state | UI assets are package data, not canonical service package | `UI/proxy_ui/README.md` | wheel data tests |
| Instance service/control/UI | Instance | HTTP ports | `9001`, `9002`, `9202` | host:port | Runtime/control/UI | vLLM forwarding, capability reporting, resource dashboard | No canonical trace context from Proxy to Instance yet | `instance/README.md` | demo instance |
| KDN Server | KDN | HTTP port | `9101` | host:port | Knowledge service | Knowledge registration, lookup, KVCache build/injection utilities | Contract shims preserve old imports | `kdn_server/README.md` | contract tests/demos |
| Request fields | Client/Scheduler/Proxy | JSON | OpenAI-compatible defaults | `model`, `messages` or `prompt`, `max_tokens`, `stream`, `RAG`, optional `Injection_type` | Request body | Routes and executes LLM request | Existing wire field names remain unchanged | `core/request.py`, README API section | demos |
| SSE metadata | Scheduler/Proxy/Instance | streaming response | Existing OpenAI-compatible streaming | OpenAI-compatible chunks plus current component behavior | Response stream | Delivers streamed tokens | No canonical trace is returned to clients | component source | demos |
| Feature switches | core config and CLI flags | Python constants/CLI | see component READMEs | booleans/strings/ports | Demo/runtime startup | Enable mock mode, UI, strategies, resource monitor | Do not change defaults in documentation PRs | `core/config.py`, demo scripts | source checkout tests |
