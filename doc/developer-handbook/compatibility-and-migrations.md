# Compatibility and migrations

| Old path | Canonical path | Shim type | Object identity | Wire compatibility | Focused tests | Source checkout | Editable install | Wheel install | Dynamic/module-string references | Service entrypoints | Removal condition | Status |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| `cacheroute_compat.runtime` | `cacheroute.compat.runtime` | Python forwarding module | Required: exported objects must be identical | Runtime profile aliases and Redis key helpers unchanged | `test/test_runtime_compat.py`, `test/test_wheel_install.py` | importable from checkout | importable | importable | Narrowly allowlisted | none | Approved migration milestone, no date invented | Transitional |
| `core.runtime_compat` | `cacheroute.compat.runtime` | Root compatibility module | Preserve helper behavior | Preserve legacy runtime strings | runtime compatibility tests | importable | importable | root packages still installed | narrow references | root demos | Later core ownership audit | Transitional |
| `kdn_server.contracts.*` | `cacheroute.contracts.v1.*` | Forwarding contract shims | Required for canonical classes/enums | Required for KDN/Gateway v1 JSON fields | `test/test_contract_foundation.py`, `test/test_contract_service_migration.py` | importable | importable | importable | allowlisted in governance | KDN root service | Contract migration closeout milestone | Transitional |
| root `scheduler`, `proxy`, `instance`, `kdn_server` | future `cacheroute.services.*` | Not a shim; current implementation | N/A | Existing HTTP/wire behavior must remain | demos/governance | root source bootstraps | installed as configured | installed as configured | Uvicorn/module strings must be audited before migration | demo scripts and CLIs | Focused service migration Issues | Transitional |
| `scheduler.strategy`, `proxy.strategy`, `proxy.queue` | future reusable `cacheroute.routing` where approved | Current implementation plus canonical foundation | N/A | Request semantics unchanged | namespace/governance tests | importable | importable | installed | strategy strings must be audited | demo flags | Focused routing migration | Transitional |
| Markdown relative links | repository docs | repository-only references | N/A | N/A | `test/test_documentation_governance.py` | must resolve | N/A | docs absent from runtime packages | update links with moves | N/A | Always maintained | Current |

Documentation migrations must audit normal imports, dynamic imports, Uvicorn/FastAPI targets, subprocess commands, Docker/Compose/CI paths, tests, packaging, README links, and Markdown relative links before moving files. No removal dates are approved here.
