# Public API and data-model catalog

This catalog covers current canonical modules under `src/cacheroute` with non-empty explicit `__all__`. It does not make private helpers stable APIs and does not flatten `cacheroute.observability.v1` into `cacheroute.observability`.

## Common rules

| Area | Current rule |
|---|---|
| Stability | Dependency-light foundation APIs are Current but still subject to migration governance. Compatibility shims are Transitional. |
| Serialization | Pydantic models use frozen, extra-forbid validation and JSON-compatible `model_dump(mode="json")` / `model_dump_json()` behavior where applicable. |
| Runtime profile values | `v1`, `legacy`, `test/mock`; `auto` is startup-only and must be resolved before persistence or propagation. |
| ID syntax | `artifact_<32 hex>`, `observation_<32 hex>`, `endpoint_<32 hex>`, `cacheop_<32 hex>`, `queuework_<32 hex>`, and `trace_<32 hex>` where enforced by models. |
| Compatibility aliases | `cacheroute_compat.runtime` forwards object identities from `cacheroute.compat.runtime`; `kdn_server.contracts` forwards canonical contract objects. |

## Module export index

| Import path | Owner | Stability | Intended use | Non-goals | Exported public objects | Focused tests |
|---|---|---|---|---|---|---|
| `cacheroute.cache` | `cache` | Current | Import the listed dependency-light objects. | Do not use service internals or private helpers as stable API. | `CacheArtifact, CacheOperationState, CacheOperationTask, CacheOperationType, CacheReplicaObservation, ObservationConfidence, ObservationSource, ObservationState` | `test/test_namespace_layout.py`, `test/test_wheel_install.py`, `test/observability`, contract tests |
| `cacheroute.cache.models` | `cache` | Current | Import the listed dependency-light objects. | Do not use service internals or private helpers as stable API. | `CacheArtifact, CacheOperationState, CacheOperationTask, CacheOperationType, CacheReplicaObservation, ObservationConfidence, ObservationSource, ObservationState` | `test/test_namespace_layout.py`, `test/test_wheel_install.py`, `test/observability`, contract tests |
| `cacheroute.compat.runtime` | `runtime` | Current / Transitional | Import the listed dependency-light objects. | Do not use service internals or private helpers as stable API. | `RUNTIME_PROFILE_AUTO, RUNTIME_PROFILE_LEGACY, RUNTIME_PROFILE_TEST_MOCK, RUNTIME_PROFILE_V1, SUPPORTED_RUNTIME_PROFILES, classify_lmcache_redis_key, filter_supported_keys, normalize_runtime_profile, resolve_scan_match` | `test/test_namespace_layout.py`, `test/test_wheel_install.py`, `test/observability`, contract tests |
| `cacheroute.contracts` | `contracts` | Current | Import the listed dependency-light objects. | Do not use service internals or private helpers as stable API. | `v1` | `test/test_namespace_layout.py`, `test/test_wheel_install.py`, `test/observability`, contract tests |
| `cacheroute.contracts.v1.cache_service` | `contracts` | Current | Import the listed dependency-light objects. | Do not use service internals or private helpers as stable API. | `ArtifactRequest, GetCacheObservationRequest, LookupArtifactRequest, LookupTokensRequest, OperationIntentRequest, CreatePrefetchIntentRequest, CreatePinIntentRequest, CreateUnpinIntentRequest, CreateClearIntentRequest, CreateRebuildIntentRequest, GetOperationStatusRequest, CancelOperationRequest, GetLMCacheEndpointsRequest, GetTierAndAdapterSummaryRequest, GetMaintenanceStatusRequest, TokenCoverage, SummaryBase, AdapterSummary, TierLevel, CapacityUsageObservation, TierSummary, MaintenanceSummary, CacheServiceResponse, GatewayTargetedResponse, GetCacheObservationResponse, LookupArtifactResponse, LookupTokensResponse, OperationResponse, CreatePrefetchIntentResponse, CreatePinIntentResponse, CreateUnpinIntentResponse, CreateClearIntentResponse, CreateRebuildIntentResponse, GetOperationStatusResponse, CancelOperationResponse, GetLMCacheEndpointsResponse, GetTierAndAdapterSummaryResponse, GetMaintenanceStatusResponse, INTENT_OPERATION_TYPES` | `test/test_namespace_layout.py`, `test/test_wheel_install.py`, `test/observability`, contract tests |
| `cacheroute.contracts.v1.common` | `contracts` | Current | Import the listed dependency-light objects. | Do not use service internals or private helpers as stable API. | `KDN_CONTRACT_VERSION, GATEWAY_CONTRACT_VERSION, ENDPOINT_ID_PATTERN, SupportState, utc_now, ContractModel, VersionedMessage, GatewayTargetedRequest, TokenReference, TokenInput` | `test/test_namespace_layout.py`, `test/test_wheel_install.py`, `test/observability`, contract tests |
| `cacheroute.contracts.v1.errors` | `contracts` | Current | Import the listed dependency-light objects. | Do not use service internals or private helpers as stable API. | `OutcomeCode, ContractErrorDetail, ContractError` | `test/test_namespace_layout.py`, `test/test_wheel_install.py`, `test/observability`, contract tests |
| `cacheroute.contracts.v1.knowledge` | `contracts` | Current | Import the listed dependency-light objects. | Do not use service internals or private helpers as stable API. | `KnowledgeDescriptor, KnowledgeResponse, RegisterKnowledgeRequest, RegisterKnowledgeResponse, UpdateKnowledgeRequest, UpdateKnowledgeResponse, ResolveKnowledgeRequest, ResolveKnowledgeResponse, ListCompatibleArtifactsRequest, ListCompatibleArtifactsResponse, QueryArtifactCompatibilityRequest, QueryArtifactCompatibilityResponse, ReportRequestOutcomeRequest, ReportRequestOutcomeResponse` | `test/test_namespace_layout.py`, `test/test_wheel_install.py`, `test/observability`, contract tests |
| `cacheroute.observability` | `observability` | Current | Import the listed dependency-light objects. | Do not use service internals or private helpers as stable API. | `TraceClock, SystemTraceClock, ManualTraceClock, TraceCollector, project_legacy_proxy_trace, RESERVED_TRACE_HEADERS, TracePropagationError, create_trace_context, decode_trace_headers, encode_trace_headers, is_trace_sampled, new_trace_id, parse_sample_rate, ObservabilityStartupConfig, resolve_observability_startup, sample_rate_warning_reason` | `test/test_namespace_layout.py`, `test/test_wheel_install.py`, `test/observability`, contract tests |
| `cacheroute.observability.clock` | `observability` | Current | Import the listed dependency-light objects. | Do not use service internals or private helpers as stable API. | `TraceClock, SystemTraceClock, ManualTraceClock` | `test/test_namespace_layout.py`, `test/test_wheel_install.py`, `test/observability`, contract tests |
| `cacheroute.observability.collector` | `observability` | Current | Import the listed dependency-light objects. | Do not use service internals or private helpers as stable API. | `TraceCollector` | `test/test_namespace_layout.py`, `test/test_wheel_install.py`, `test/observability`, contract tests |
| `cacheroute.observability.legacy_proxy` | `observability` | Current | Import the listed dependency-light objects. | Do not use service internals or private helpers as stable API. | `project_legacy_proxy_trace` | `test/test_namespace_layout.py`, `test/test_wheel_install.py`, `test/observability`, contract tests |
| `cacheroute.observability.propagation` | `observability` | Current | Import the listed dependency-light objects. | Do not use service internals or private helpers as stable API. | `REQUEST_ID_HEADER, TRACE_VERSION_HEADER, TRACE_ID_HEADER, RUNTIME_PROFILE_HEADER, TRACE_SAMPLED_HEADER, TRACE_CREATED_AT_HEADER, RESERVED_TRACE_HEADERS, TracePropagationError, parse_sample_rate, _MAX_AGE, _FUTURE_SKEW_TOLERANCE, is_trace_sampled, new_trace_id, create_trace_context, encode_trace_headers, decode_trace_headers` | `test/test_namespace_layout.py`, `test/test_wheel_install.py`, `test/observability`, contract tests |
| `cacheroute.observability.startup` | `observability` | Current | Import the listed dependency-light objects. | Do not use service internals or private helpers as stable API. | `ObservabilityStartupConfig, resolve_observability_startup, sample_rate_warning_reason` | `test/test_namespace_layout.py`, `test/test_wheel_install.py`, `test/observability`, contract tests |
| `cacheroute.observability.v1` | `observability` | Current | Import the listed dependency-light objects. | Do not use service internals or private helpers as stable API. | `TraceContext, TraceComponent, TraceStageName, TraceStageState, TraceValueKind, TraceProvenance, TraceMeasurement, TraceStage, RequestTrace, CacheOperationTrace, OperationWaiterLink, OperationWaiterState` | `test/test_namespace_layout.py`, `test/test_wheel_install.py`, `test/observability`, contract tests |
| `cacheroute.observability.v1.enums` | `observability` | Current | Import the listed dependency-light objects. | Do not use service internals or private helpers as stable API. | `TraceComponent, TraceStageName, TraceStageState, TraceValueKind, OperationWaiterState` | `test/test_namespace_layout.py`, `test/test_wheel_install.py`, `test/observability`, contract tests |
| `cacheroute.observability.v1.models` | `observability` | Current | Import the listed dependency-light objects. | Do not use service internals or private helpers as stable API. | `TraceContext, TraceProvenance, TraceMeasurement, TraceStage, RequestTrace, CacheOperationTrace, OperationWaiterLink` | `test/test_namespace_layout.py`, `test/test_wheel_install.py`, `test/observability`, contract tests |
| `cacheroute.routing` | `routing` | Current | Import the listed dependency-light objects. | Do not use service internals or private helpers as stable API. | `QueueState, QueueWork` | `test/test_namespace_layout.py`, `test/test_wheel_install.py`, `test/observability`, contract tests |
| `cacheroute.routing.queue` | `routing` | Current | Import the listed dependency-light objects. | Do not use service internals or private helpers as stable API. | `QueueState, QueueWork` | `test/test_namespace_layout.py`, `test/test_wheel_install.py`, `test/observability`, contract tests |
| `cacheroute.runtime` | `runtime` | Current | Import the listed dependency-light objects. | Do not use service internals or private helpers as stable API. | `RuntimeProfile` | `test/test_namespace_layout.py`, `test/test_wheel_install.py`, `test/observability`, contract tests |
| `cacheroute.runtime.profiles` | `runtime` | Current | Import the listed dependency-light objects. | Do not use service internals or private helpers as stable API. | `RuntimeProfile` | `test/test_namespace_layout.py`, `test/test_wheel_install.py`, `test/observability`, contract tests |
| `cacheroute.runtime.state` | `runtime` | Current | Import the listed dependency-light objects. | Do not use service internals or private helpers as stable API. | `Snapshot, StateTransitionError, StrEnum` | `test/test_namespace_layout.py`, `test/test_wheel_install.py`, `test/observability`, contract tests |
| `cacheroute.topology` | `topology` | Current | Import the listed dependency-light objects. | Do not use service internals or private helpers as stable API. | `LMCacheEndpoint, LMCacheGatewayProfile` | `test/test_namespace_layout.py`, `test/test_wheel_install.py`, `test/observability`, contract tests |
| `cacheroute.topology.lmcache` | `topology` | Current | Import the listed dependency-light objects. | Do not use service internals or private helpers as stable API. | `LMCacheEndpoint, LMCacheGatewayProfile` | `test/test_namespace_layout.py`, `test/test_wheel_install.py`, `test/observability`, contract tests |

## Core data models and wire values

| Module | Important fields, defaults, validation, enums | Minimal Python 3 example |
|---|---|---|
| `cacheroute.runtime` | `RuntimeProfile`: `v1`, `legacy`, `test/mock`, `auto`; `auto` is accepted only by `resolve_startup`; `resolve_startup("auto", v1_available=False)` returns `legacy`. | `from cacheroute.runtime import RuntimeProfile; assert RuntimeProfile.normalize("modern").value == "v1"` |
| `cacheroute.topology` | `LMCacheEndpoint` validates non-empty `name` and `compatibility_profile_id`, canonicalizes `endpoint_id` from name, default `runtime_profile=v1`, `generation=1`; `LMCacheGatewayProfile` values include `mp_http_api`, `mp_coordinator`, `mp_sdk`, `mp_metrics_events`, `legacy_gateway`, `mock`, `unknown_future`. | `from cacheroute.topology import LMCacheEndpoint, LMCacheGatewayProfile; ep=LMCacheEndpoint(name="local", gateway_profile=LMCacheGatewayProfile.MOCK, compatibility_profile_id="mock"); print(ep.endpoint_id)` |
| `cacheroute.cache` | Cache artifact identity is canonical and immutable. Observation states: `pending`, `available`, `unavailable`, `unknown`, `partial`. Operation types: `lookup`, `prefetch`, `pin`, `unpin`, `clear`, `rebuild`, `observe`. Operation states: `pending`, `running`, `retry_wait`, `succeeded`, `failed`, `cancelled`. | `from cacheroute.cache import CacheOperationState; assert CacheOperationState.SUCCEEDED.terminal` |
| `cacheroute.routing` | `QueueWork` validates `queuework_` and `cacheop_` IDs, UTC timestamps, and legal state transitions for `queued`, `claimed`, `executing`, `retry_wait`, `completed`, `failed`, `cancelled`. | `from cacheroute.routing import QueueState; assert QueueState.RETRY_WAIT.retryable` |
| `cacheroute.contracts.v1` | `contract_version` defaults to `kdn.v1`; Gateway version is `lmcache-gateway.v1`; models are frozen and reject extra fields; `runtime_profile=auto` is invalid on persisted messages; endpoint generation zero is Legacy-only. | `from cacheroute.contracts.v1 import KDN_CONTRACT_VERSION; assert KDN_CONTRACT_VERSION == "kdn.v1"` |
| `cacheroute.observability` | PR #183 startup exports include `ObservabilityStartupConfig`, `resolve_observability_startup`, and sample-rate warning helpers. Propagation uses reserved headers: `scheduler-request-id`, `x-cacheroute-trace-version`, `x-cacheroute-trace-id`, `x-cacheroute-runtime-profile`, `x-cacheroute-trace-sampled`, `x-cacheroute-trace-created-at`. Scheduler overwrites them; Proxy validates or falls back locally. | `from cacheroute.observability import create_trace_context, encode_trace_headers; from cacheroute.runtime import RuntimeProfile; print(encode_trace_headers(create_trace_context("1", RuntimeProfile.LEGACY)))` |
| `cacheroute.observability.v1` | Schema version is `observability.v1`; components include `client`, `scheduler`, `proxy`, `kdn`, `gateway`, `instance`, `vllm`, `lmcache`, `legacy_adapter`, `test`; value kinds include `predicted`, `desired`, `observed`, `measured`, `actual`, `inferred`, `legacy_projected`; stage names include Proxy prepare/ready queue, first token, decode, completion, and future non-current names. | `from cacheroute.observability.v1 import TraceValueKind; assert TraceValueKind.OBSERVED.value == "observed"` |
| `cacheroute.compat` and `cacheroute_compat` | Transitional runtime compatibility exports include `RUNTIME_PROFILE_AUTO`, `RUNTIME_PROFILE_LEGACY`, `RUNTIME_PROFILE_TEST_MOCK`, `RUNTIME_PROFILE_V1`, `SUPPORTED_RUNTIME_PROFILES`, `normalize_runtime_profile`, `resolve_scan_match`, `classify_lmcache_redis_key`, and `filter_supported_keys`. | `from cacheroute_compat.runtime import normalize_runtime_profile; assert normalize_runtime_profile("modern") == "v1"` |

## Current observability propagation and startup exports

Status: Current. Scheduler-to-Proxy propagation is active; Proxy-local collection observes prepare queue, ready queue, first response, decode, and completion. Collection is process-local and does not return a canonical trace to clients or propagate context to Instance. Exporters, persistence, debug registries, Gateway, LMCache, vLLM, and Instance instrumentation are not Current.
